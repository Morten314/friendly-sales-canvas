---
synthesizes_review: 38-signals-cta-impl-review-2-glm-5.2.md
artifact: 38-signals-cta
artifact_type: impl
reactor_model: claude-opus-4-8
date: 2026-06-20
round: 2
---

## Round Recommendation

no

Reason: All findings are Low/Nit (0 Critical/High) and the three agreed fixes are small and self-contained — no re-review round warranted.

## Agreed Findings

- **[Low] Lock message not announced to assistive tech.** Verified: `SignalCard.tsx:335-337` renders the lock `<p>` with no `role`/`aria-live`, while the CTA carries `aria-disabled` — so an AT user hears a "disabled" button that, on activation, announces nothing, defeating spec §2's "the control can explain itself" intent. Fix: add `role="status"` (polite live region) to the lock `<p>`. Also drop the redundant `role="button"` on the native-`<button>` CTA (`SignalCard.tsx:323`) per ARIA "no redundant roles" — the implicit role already satisfies the `getByRole("button")` tests.
- **[Low] PDF footer `•` bypasses `escapePdfText` and mojibakes.** Verified: `artefactPdf.ts:101` emits a literal `•` (U+2022) into the `Tj` stream, yet `escapePdfText` already folds `•`→`-` at `:14`. Under Helvetica/WinAnsi the footer renders "Generated by Brewra AI â€¢ …". This is a missed application of the branch's own in-scope fold, **not** the deferred TD-FE-78 "residual non-ASCII beyond the fold" class. Fix: route the footer text through `escapePdfText` so the existing `•`→`-` fold applies; add a test asserting no raw `•` survives into the stream.
- **[Nit] Multi-item drain ordering untested.** Verified: `ArtifactsPage.test.tsx` only ever enqueues a single item (`:52`, `:60`, each one item), so a regression in the `≥2`-item path (`[...queued.slice().reverse(), ...prev]` + `mostRecent = queued[last]`, `ArtifactsPage.tsx:67-71`) would pass silently. Fix: add a two-item enqueue case asserting the most-recently-enqueued lands first and its folder is the one opened/expanded.

## Disagreed Findings

- None on the findings themselves — all three are accurate and confirmed against current code.
- **One factual correction to GLM's Observations (not a finding):** GLM states (line 44) that review-1's accept-dismiss finding "was read from a truncated diff that omitted the `[isAccepted]` effect." That is inaccurate. The `[isAccepted]` effect was **genuinely absent** at review-1 time and was **added as the round-1 synthesis fix** (commit `5fd84a3`); review-1 correctly identified a real gap that has since been closed. GLM's *conclusion* (the effect is present and correct now) is right; its *history* (a truncation artifact) is not. Recorded so the trail stays accurate.

## Deferred Findings

- None. All three findings are being fixed in this round.

## Severity Disagreements

- None — GLM's Low/Low/Nit calibration is accurate. Note this *reverses my round-1 synthesis*, which deferred the footer `•` as a Nit folded into TD-FE-78. GLM's argument is the better one: `•` is explicitly in the fold set this branch introduced, so the footer is an in-scope miss, not deferred generator debt. I re-accept it as a Low and am fixing it.

## Open Questions

- **Round-numbering bookkeeping.** GLM's frontmatter self-labels `round: 1` ("second parallel reviewer joining the in-progress round 1"), but the file is named `…-impl-review-2-…` and a round-1 synthesis already exists. This synthesis is filed as round 2 per the filename N. No action — noted so the review trail is unambiguous (round 1 = Opus fresh-eyes; round 2 = GLM-5.2 second reviewer).
