# Committed for Lovable preview + GitHub sync (spec 42).
# Lovable requires VITE_* vars in a tracked .env — do NOT gitignore this file.
# For local uvicorn overrides, copy .env.local.example → .env.local (gitignored).

VITE_API_BASE_URL=/api
VITE_BACKEND_BASE_URL=https://brewra-gtm-intelligence-1.onrender.com
