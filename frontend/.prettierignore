dist
dev-dist
node_modules
playwright-report
coverage
e2e/**/*-snapshots/**
package-lock.json

# Prettier wraps multi-line expect(...) such that an above-line @ts-expect-error
# directive no longer targets the type-error-bearing call. Skip until the test
# is rewritten to be whitespace-insensitive.
src/lib/__tests__/marketScoreDescriptions.test.ts
