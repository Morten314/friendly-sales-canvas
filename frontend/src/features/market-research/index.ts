// Public surface for the `market-research` feature.
// Re-export ONLY what other features may consume; internals stay unexported.
// Cross-feature consumers import from "@/features/market-research", never a deep path.
export {};
