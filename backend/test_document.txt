This is a test document for the document upload and embedding system.

The system should:
1. Upload this file to S3
2. Process it in the background
3. Convert it to embeddings
4. Store embeddings in Pinecone

This document contains some sample text to test the embedding generation process.
The text splitter should break this into chunks of 1000 characters with 200 character overlap.

Machine learning and artificial intelligence are transforming how we process and understand documents.
Vector embeddings allow us to search and retrieve information semantically rather than just by keywords.

This test will verify that:
- File uploads successfully to S3
- Background task processes the file
- Embeddings are generated correctly
- Status updates are tracked in MongoDB

Let's see if everything works as expected!
