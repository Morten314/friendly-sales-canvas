# Optional local overrides (copy to .env.local — gitignored, takes precedence over .env).
# Use 127.0.0.1 (NOT localhost) on Windows so the Vite proxy reaches IPv4-bound uvicorn.

VITE_API_BASE_URL=/api
VITE_BACKEND_BASE_URL=http://127.0.0.1:8000
